machine:
  node:
    version: 6.2.0
test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - npm -v
    - node -v
  override:
    - sitespeed.io -u http://www.fa7.edu.br --deep 0 --file urls.txt --junit > $CIRCLE_TEST_REPORTS/junit/sitespeedio_result.xml
